

def dump_hidden_states(path, epoch, hidden_states, hidden_size):

	""" format: 
		- one row per line {depth1, depth2, x1, y1, x2, y2 }
	"""
	pass


def load_hidden_states(path, epoch):
	pass

def visualize_hidden_states():
	pass